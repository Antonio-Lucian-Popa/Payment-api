/**
 * ⚠️  ACEST FIȘIER A FOST MUTAT
 * =============================
 * Exemplele de integrare și referința completă de API se află acum în API.md
 * (întreținut și actualizat: autentificare x-api-key, webhook forwarding, erori).
 *
 *   👉  Vezi ./API.md
 *
 * Cuprins API.md:
 *   - Base URL & autentificare (x-api-key)
 *   - Rate limiting și format sume
 *   - Toate endpoint-urile (request / response / erori)
 *   - Integrare frontend (vanilla JS, React, curl)
 *   - Webhook forwarding către backend-urile tale + verificarea semnăturii
 *   - Flow-ul complet al plății
 *
 * Acest fișier .js este păstrat doar ca redirect, pentru compatibilitate cu
 * link-urile mai vechi. Nu conține cod care se execută.
 */

console.log('Exemplele s-au mutat în API.md - vezi ./API.md');
